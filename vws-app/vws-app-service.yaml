apiVersion: v1
kind: Service
metadata:
  name: vwsnode-service # vwsnode-service.default.svc.cluster.local
  namespace: vws-app
spec:
  type: LoadBalancer
  ports:
  - port: 3000
  selector:
    app: vwsnode-app